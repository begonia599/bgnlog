# 海棠小栈 · 功能长期规划（ROADMAP）

> 状态：**待审阅**。本文档只描述计划，未执行任何功能改动。经确认后按里程碑逐项落地。
> 最后更新：2026-06-21

## 指导原则

- **复用已有机制**：后端已能"按请求 Host 注入"（`serveIndexWithOG`），RSS / sitemap / 逐篇 OG 都沿用同一套思路，零硬编码域名。
- **不破坏现有架构**：业务/平台分离、`type`(post/note) 分型、中央鉴权等保持不变。
- **每个里程碑独立可交付、可验证**，不做大爆炸式重构。
- **按写作习惯裁剪**：M1–M2 普适必做；M3–M4 按你实际需求挑。

## 现有功能盘点（作为基线）

已具备：文稿/手记/时光分型、分类/标签、草稿、Markdown 编辑器、封面图、嵌套评论、全文搜索（PG tsvector）、专区(Discord 门禁)、中央 SSO/OAuth + 角色权限、友链、Hero(一言+Discord presence)、暗色模式、移动端汉堡导航、OG 标签(站点级)、`lang=zh-CN`、滚动条占位修复。

明显缺口：**RSS、代码高亮、sitemap/robots、阅读时长、文章目录(TOC)、相关文章、分享、数学/图表、分析**。

---

## M1 —— 分发与 SEO 基建（最高性价比，建议首先做）

目标：让搜索引擎能收录、让极客能用 RSS 订阅。与刚做的 OG 一脉相承。

| # | 功能 | 触及 | 工作量 | 实现要点 |
|---|------|------|--------|----------|
| 1.1 | **RSS / Atom feed** | 后端 | S | 新增 `GET /feed.xml`（默认 type=post），按 Host 生成绝对链接；可选 `GET /notes.xml`。从 published 文章取最近 N 篇，输出 RSS 2.0 或 Atom。复用 `serveIndexWithOG` 的 origin 探测逻辑抽成公共函数。 |
| 1.2 | **sitemap.xml + robots.txt** | 后端 | S | `GET /sitemap.xml` 列出文章/手记/专区等公开 URL（Host 感知）；`GET /robots.txt` 指向 sitemap。注意 release 模式下与 SPA fallback 路由的优先级。 |
| 1.3 | **逐篇文章 OG / JSON-LD** | 后端 | M | 扩展 SPA fallback：当路径形如 `/article/:slug` 时按 slug 查库，注入该文标题/摘要/封面到 `og:*` 与 `<script type="application/ld+json">`(Article schema)。这是早先 OG 工作里标注的"留待以后"项。 |

**验收**：`curl /feed.xml` 得到合法 RSS 且链接为绝对地址；Google Rich Results / RSS 阅读器可解析；分享单篇文章显示该文标题与封面。

---

## M2 —— 文章阅读体验

目标：技术文章读起来"对味"。主要在前端 Markdown 渲染管线。

| # | 功能 | 触及 | 工作量 | 实现要点 |
|---|------|------|--------|----------|
| 2.1 | **代码语法高亮** | 前端 | S-M | 现有 `react-markdown + remark-gfm + rehype-raw` 管线加 `rehype-highlight`（轻）或 `rehype-pretty-code`/Shiki（更美，构建期）。需在 `.article-content` 主题里适配亮/暗色。 |
| 2.2 | **代码块"复制"按钮** | 前端 | S | 渲染 `pre` 时挂一个复制按钮组件。 |
| 2.3 | **阅读时长** | 前端 | S | 按正文字数估算（中文按字、英文按词），显示在文章头部。可前端算或后端随文章返回。 |
| 2.4 | **文章目录 TOC + 滚动高亮** | 前端 | M | 从 Markdown 标题生成锚点目录，桌面端侧栏吸附、scroll-spy 高亮当前节；移动端折叠。 |
| 2.5 | **数学公式 KaTeX / Mermaid 图**（可选） | 前端 | M | `remark-math + rehype-katex`；Mermaid 客户端渲染。仅在你写算法/技术图解时需要。 |

**验收**：代码块有高亮+复制；长文显示阅读时长与可点击目录；（若启用）公式与流程图正确渲染；暗色模式下均正常。

---

## M3 —— 内容发现与互动

| # | 功能 | 触及 | 工作量 | 实现要点 |
|---|------|------|--------|----------|
| 3.1 | **相关文章推荐** | 后端+前端 | M | 按共享标签/同分类取 Top N（SQL 按标签交集计数排序），文章底部展示。 |
| 3.2 | **分享按钮** | 前端 | S | 复制链接 / 微信(二维码) / X 等;复用已注入的 OG。 |
| 3.3 | **系列 / 合集** | 后端+前端 | M | 给文章加 `series` 字段或独立表,文章内显示同系列导航。 |
| 3.4 | **评论邮件通知** | 后端(+平台?) | M-L | 有人回复时发邮件。需 SMTP 配置;若走中央平台的通知能力则需先评估平台是否提供。 |
| 3.5 | **轻量访问分析** | 部署 | S-M | 自托管 Umami,或后端简单 PV 统计页。隐私友好,不接第三方。 |

**验收**：文章底部出现相关推荐;分享卡片正确;(若做)系列内可前后跳转、评论回复有邮件。

---

## M4 —— 极客特色（可选，按兴趣）

| # | 功能 | 工作量 | 备注 |
|---|------|--------|------|
| 4.1 | **Now / 动态页** | M | 扩展已有 Discord presence,做成"我在干嘛/最近在听/在读"。 |
| 4.2 | **书架 / 项目页** | S-M | 静态或后端管理的展示页,呼应友链。 |
| 4.3 | **PWA / 离线** | M | manifest + service worker,可安装、离线读已访问文章。 |
| 4.4 | **实时"正在阅读"** | L | WebSocket 显示当前在线/同读人数(Shiro 风格),架构成本较高。 |
| 4.5 | **Webmention / 互联** | M | 接收外站提及,IndieWeb 玩法。 |

---

## 建议执行顺序

1. **M1 全部**（分发+SEO 基建,和 OG 同源,最快见效）
2. **M2.1–2.4**（阅读体验,技术博客体感提升最大）
3. 之后按写作习惯从 M3 挑（相关文章 + 分享 优先），M4 当作长期兴趣项。

> 不在范围内（已决定不做或已完成）：非管理员细粒度权限已通过"注册声明授权"机制解决;归档页已移除（与时光重复）。

## 待你拍板

- 里程碑优先级是否调整？
- M1.3 逐篇 OG、M2.5 数学公式、M3.4 评论邮件 这几项**取决于你的内容类型和是否愿意配 SMTP**，要不要纳入？
- 确认后从哪个里程碑开始执行？
